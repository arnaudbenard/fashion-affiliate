const React = require('react');

module.exports = React.createClass({

    render() {
        return (
            <div>
                <h1 className="center">Cool stuff</h1>
                <h2 className="center">Awesome, isn't it?</h2>
            </div>
        );
    }
});
